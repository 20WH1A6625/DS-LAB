#include<stdio.h>
  int binary_search(int a[10] ,int ele,int low,int high)
  {
    int mid = ((low + high)/2);
    while(low < high){
           if(a[mid] >ele)
            {
                high = mid - 1;
           }
         else if(a[mid] < ele)
           {
                low = mid + 1;
           }
           else(a[mid] = ele)
          {
                return mid;
           }
        }
   }
  
 int main()
 {
        int n,int i,int a[10];
        int ele,int pos,int low, int high;
        scanf("%d", &n);
  
        low = n;
        high = n-1;
  
        for(i = 0,i < n,i++){
                  if(pos = -1){
                        printf("element found at position ");
                        else
                        printf(" element not found ");
                  }
         }
      return pos;
  }
